'use strict';
const fs=require('fs');const path=require('path');const assert=require('assert');const read=name=>fs.readFileSync(path.join(__dirname,'..','migrations',name),'utf8');const sql=read('20260827_bbs_phase10a_mobile_batch_observation.sql'),rollback=read('20260827_bbs_phase10a_mobile_batch_observation.rollback.sql');for(const table of ['BBS_Observation_Batches','BBS_Observation_Batch_Members'])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${table}`));for(const key of ['uq_bbs_batch_idempotency','uq_bbs_batch_member_employee','uq_bbs_batch_member_observation'])assert.ok(sql.includes(key),`missing ${key}`);for(const flag of ['batch_observation_enabled','mobile_observation_wizard_enabled','draft_autosave_enabled']){assert.ok(sql.includes(`('${flag}', '1')`));assert.ok(rollback.includes(`('${flag}', '0')`));}assert.doesNotMatch(rollback,/DROP\s+TABLE|DELETE\s+FROM/i);console.log('BBS Phase 10A migration and safe rollback contract: PASS');
