'use strict';
const assert=require('assert'),fs=require('fs'),os=require('os'),path=require('path');const{spawn}=require('child_process');require('dotenv').config({path:path.join(__dirname,'..','.env')});
const appUrl=process.env.BBS_PHASE4_APP_URL||'http://127.0.0.1:5500/index.html',apiUrl=String(process.env.BBS_PHASE4_API_URL||'http://127.0.0.1:5001').replace(/\/+$/,''),chromePath=process.env.BBS_PHASE4_BROWSER||'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',port=Number(process.env.BBS_PHASE4_CDP_PORT||9844),profile=fs.mkdtempSync(path.join(os.tmpdir(),'tsh-bbs-phase4-browser-')),errors=[];let chrome,cdp;const sleep=ms=>new Promise(r=>setTimeout(r,ms));
class Cdp{constructor(url){this.id=1;this.pending=new Map();this.socket=new WebSocket(url);}async connect(){this.socket.addEventListener('message',async event=>{let raw=event.data;if(raw&&typeof raw.text==='function')raw=await raw.text();if(raw instanceof ArrayBuffer)raw=Buffer.from(raw).toString('utf8');const message=JSON.parse(String(raw));if(message.method==='Runtime.exceptionThrown')errors.push(message.params?.exceptionDetails?.exception?.description||message.params?.exceptionDetails?.text||'Runtime exception');if(message.method==='Runtime.consoleAPICalled'&&message.params?.type==='error')errors.push((message.params.args||[]).map(a=>a.value||a.description||'').join(' '));const pending=this.pending.get(message.id);if(!pending)return;this.pending.delete(message.id);clearTimeout(pending.timer);message.error?pending.reject(new Error(message.error.message)):pending.resolve(message.result);});await new Promise((resolve,reject)=>{const timer=setTimeout(()=>reject(new Error('CDP connect timeout')),15000);this.socket.addEventListener('open',()=>{clearTimeout(timer);resolve();},{once:true});this.socket.addEventListener('error',reject,{once:true});});}command(method,params={},timeout=60000){const id=this.id++;return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{this.pending.delete(id);reject(new Error(`CDP timeout: ${method}`));},timeout);this.pending.set(id,{resolve,reject,timer});this.socket.send(JSON.stringify({id,method,params}));});}close(){try{this.socket.close();}catch{}}}
async function evaluate(expression){const result=await cdp.command('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.exception?.description||result.exceptionDetails.text);return result.result?.value;}async function waitFor(expression,timeout=45000){const started=Date.now();while(Date.now()-started<timeout){if(await evaluate(expression))return;await sleep(300);}const diagnostic=await evaluate(`({href:location.href,ready:document.readyState,token:Boolean(localStorage.getItem('tsh_token')),user:Boolean(localStorage.getItem('tsh_user')),body:(document.body?.innerText||'').slice(0,300)})`).catch(error=>({diagnosticError:error.message}));throw new Error(`Timed out: ${expression}\nDiagnostic: ${JSON.stringify(diagnostic)}\nBrowser errors: ${errors.join(' | ')}`);}
async function main(){assert.ok(fs.existsSync(chromePath));const login=await fetch(`${apiUrl}/api/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({employeeId:process.env.PROD_UAT_ADMIN_ID,password:process.env.PROD_UAT_ADMIN_PASSWORD})}),session=await login.json();assert.strictEqual(login.status,200,JSON.stringify(session));chrome=spawn(chromePath,['--headless=new','--disable-gpu','--no-sandbox','--disable-dev-shm-usage','--disable-extensions','--no-first-run','--remote-allow-origins=*','--window-size=1920,1080',`--remote-debugging-port=${port}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','ignore'],windowsHide:true});let targets;for(let i=0;i<60;i++){try{const r=await fetch(`http://127.0.0.1:${port}/json`);if(r.ok){targets=await r.json();break;}}catch{}await sleep(250);}const page=targets?.find(x=>x.type==='page');assert.ok(page?.webSocketDebuggerUrl);cdp=new Cdp(page.webSocketDebuggerUrl.replace('://localhost:','://127.0.0.1:'));await cdp.connect();await cdp.command('Page.enable');await cdp.command('Runtime.enable');await cdp.command('Page.addScriptToEvaluateOnNewDocument',{source:`window.API_BASE=${JSON.stringify(`${apiUrl}/api`)};`});await cdp.command('Page.navigate',{url:appUrl});await waitFor(`location.href.startsWith(${JSON.stringify(appUrl)})&&document.readyState==='complete'`);await evaluate(`(()=>{localStorage.setItem('tsh_token',${JSON.stringify(session.token)});localStorage.setItem('tsh_user',${JSON.stringify(JSON.stringify(session.user))});location.hash='#bbs-smart-card';location.reload();return true;})()`);await waitFor(`location.hash==='#bbs-smart-card'&&document.querySelector('[data-bbs-tab="cards"]')`);await evaluate(`document.querySelector('[data-bbs-tab="cards"]').click()`);await waitFor(`document.querySelector('#bbs-template-form')&&document.querySelector('#bbs-card-employees')`);const desktop=await evaluate(`(()=>{const shell=document.querySelector('[data-bbs-shell]'),page=document.getElementById('bbs-smart-card-page'),visible=e=>e.getClientRects().length>0,ids=[...document.querySelectorAll('#bbs-smart-card-page [id]')].map(e=>e.id),duplicateIds=ids.filter((id,i)=>ids.indexOf(id)!==i),unnamedInteractive=[...document.querySelectorAll('#bbs-smart-card-page button,#bbs-smart-card-page a[href]')].filter(visible).filter(e=>!(e.getAttribute('aria-label')||e.getAttribute('title')||e.textContent.trim())),unlabelledFields=[...document.querySelectorAll('#bbs-smart-card-page input,#bbs-smart-card-page select,#bbs-smart-card-page textarea')].filter(visible).filter(e=>!(e.getAttribute('aria-label')||e.getAttribute('title')||e.getAttribute('placeholder')||(e.id&&document.querySelector('label[for="'+CSS.escape(e.id)+'"]')||e.closest('label'))));return{tabs:document.querySelectorAll('[data-bbs-tab]').length,templateForm:Boolean(document.querySelector('#bbs-template-form input[type=file]')),issue:Boolean(document.querySelector('[data-bbs-issue]')),employeeRows:document.querySelectorAll('[data-card-employee]').length,overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth+2,shellFill:shell.getBoundingClientRect().width/page.getBoundingClientRect().width,duplicateIds:duplicateIds.length,unnamedInteractive:unnamedInteractive.length,unlabelledFields:unlabelledFields.length}})()`);assert.strictEqual(desktop.tabs,6);assert.strictEqual(desktop.templateForm,true);assert.strictEqual(desktop.issue,true);assert.ok(desktop.employeeRows>0);assert.strictEqual(desktop.overflow,false);assert.ok(desktop.shellFill>.98,`BBS shell must fill the wide page; ratio=${desktop.shellFill}`);assert.strictEqual(desktop.duplicateIds,0,'BBS page must not render duplicate element IDs');assert.strictEqual(desktop.unnamedInteractive,0,'Visible BBS buttons and links need accessible names');assert.strictEqual(desktop.unlabelledFields,0,'Visible BBS form fields need a label or accessible name');await evaluate(`document.querySelector('[data-bbs-tab="actions"]').click()`);await waitFor(`document.querySelectorAll('[data-action-filter]').length===3&&document.querySelector('[data-action-reminder]')`);await evaluate(`document.querySelector('[data-bbs-tab="analytics"]').click()`);await waitFor(`document.querySelectorAll('[data-analytics-filter]').length===6&&document.querySelector('#bbs-analytics-report')`);await cdp.command('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:1,mobile:true});await sleep(700);assert.strictEqual(await evaluate(`document.documentElement.scrollWidth<=document.documentElement.clientWidth+2`),true);assert.deepStrictEqual(errors,[],`Browser errors: ${errors.join(' | ')}`);console.log(`BBS Phase 7 browser UAT: PASS (${desktop.employeeRows} card employees, full-width ${desktop.shellFill.toFixed(3)}, accessible controls, mobile no overflow)`);}
main().catch(e=>{console.error(e.stack||e);process.exitCode=1;}).finally(async()=>{cdp?.close();if(chrome&&!chrome.killed)chrome.kill();await fs.promises.rm(profile,{recursive:true,force:true}).catch(()=>{});});
