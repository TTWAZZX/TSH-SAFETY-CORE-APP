'use strict';
const assert=require('assert');const path=require('path');const{spawnSync}=require('child_process');const rules=require('../services/bbs-action');
const fixture={priorities:['critical','HIGH','Medium','x'],statuses:['open','IN PROGRESS','Pending Verification','x'],dates:['2026-08-25','2026-02-30','25/08/2026'],transitions:[['Open','In Progress'],['Open','Closed'],['In Progress','Pending Verification'],['Pending Verification','Closed'],['Pending Verification','Reopened'],['Closed','Reopened']],answerIds:[1,123456]};
const js={priorities:fixture.priorities.map(rules.normalizePriority),statuses:fixture.statuses.map(rules.normalizeStatus),dates:fixture.dates.map(rules.normalizeIsoDate),transitions:fixture.transitions.map(([a,b])=>rules.transitionAllowed(a,b)),ownerTransitions:fixture.transitions.map(([a,b])=>rules.needsOwnerPermission(a,b)),verifierTransitions:fixture.transitions.map(([a,b])=>rules.needsVerifierPermission(a,b)),actionNos:fixture.answerIds.map(rules.actionNo)};
const php=process.env.PHP_BIN||'C:\\xampp\\php\\php.exe';const result=spawnSync(php,[path.join(__dirname,'..','..','api','tests','bbs_action_fixture_runner.php')],{input:JSON.stringify(fixture),encoding:'utf8'});assert.strictEqual(result.status,0,result.stderr);assert.deepStrictEqual(JSON.parse(result.stdout),js,'Node/PHP corrective action rule parity mismatch');assert.strictEqual(rules.suppressionKey(1,'OverdueReminder','012609','2026-08-25').length,64);console.log('BBS Phase 5 Node/PHP state, SLA input and idempotency parity: PASS');
