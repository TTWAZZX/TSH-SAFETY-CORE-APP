'use strict';
const assert=require('assert');const fs=require('fs');const os=require('os');const path=require('path');const{spawn}=require('child_process');const mysql=require('mysql2/promise');const jwt=require('jsonwebtoken');require('dotenv').config({path:path.join(__dirname,'..','.env')});
const appUrl=process.env.BBS_PHASE8_APP_URL||'http://127.0.0.1:5500/index.html';const apiUrl=String(process.env.BBS_PHASE8_API_URL||'http://127.0.0.1:5001').replace(/\/+$/,'');const chromePath=process.env.BBS_PHASE8_BROWSER||'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe';const port=Number(process.env.BBS_PHASE8_CDP_PORT||9838),profile=fs.mkdtempSync(path.join(os.tmpdir(),'tsh-bbs8-browser-')),errors=[];let chrome,cdp,db;const sleep=ms=>new Promise(r=>setTimeout(r,ms));
class Cdp{constructor(url){this.id=1;this.pending=new Map();this.socket=new WebSocket(url);}async connect(){this.socket.addEventListener('message',async event=>{let raw=event.data;if(raw&&typeof raw.text==='function')raw=await raw.text();if(raw instanceof ArrayBuffer)raw=Buffer.from(raw).toString('utf8');const message=JSON.parse(String(raw));if(message.method==='Runtime.exceptionThrown')errors.push(message.params?.exceptionDetails?.exception?.description||message.params?.exceptionDetails?.text||'Runtime exception');if(message.method==='Runtime.consoleAPICalled'&&message.params?.type==='error')errors.push((message.params.args||[]).map(x=>x.value||x.description||'').join(' '));const pending=this.pending.get(message.id);if(!pending)return;this.pending.delete(message.id);clearTimeout(pending.timer);message.error?pending.reject(new Error(message.error.message)):pending.resolve(message.result);});await new Promise((resolve,reject)=>{const timer=setTimeout(()=>reject(new Error('Chrome CDP timeout')),15000);this.socket.addEventListener('open',()=>{clearTimeout(timer);resolve();},{once:true});this.socket.addEventListener('error',reject,{once:true});});}command(method,params={},timeout=60000){const id=this.id++;return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{this.pending.delete(id);reject(new Error(`CDP timeout ${method}`));},timeout);this.pending.set(id,{resolve,reject,timer});this.socket.send(JSON.stringify({id,method,params}));});}close(){try{this.socket.close();}catch{}}}
async function evaluate(expression){const result=await cdp.command('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.exception?.description||result.exceptionDetails.text);return result.result?.value;}async function waitFor(expression,timeout=45000){const started=Date.now();while(Date.now()-started<timeout){if(await evaluate(expression))return;await sleep(300);}throw new Error(`Timed out: ${expression}`);}
const tokenFor=row=>jwt.sign({id:row.EmployeeID,name:row.EmployeeName,role:row.Role,department:row.Department,unit:row.Unit,position:row.Position},process.env.JWT_SECRET,{expiresIn:'30m'});const userPayload=row=>({id:row.EmployeeID,EmployeeID:row.EmployeeID,name:row.EmployeeName,EmployeeName:row.EmployeeName,role:row.Role,Role:row.Role,department:row.Department,Department:row.Department,unit:row.Unit,Unit:row.Unit,position:row.Position,Position:row.Position});
async function enter(row){await evaluate(`(()=>{localStorage.setItem('tsh_token',${JSON.stringify(tokenFor(row))});localStorage.setItem('tsh_user',${JSON.stringify(JSON.stringify(userPayload(row)))});location.hash='#bbs-smart-card';location.reload();return true;})()`);await sleep(3500);await waitFor(`document.querySelector('[data-bbs-shell]') && document.querySelector('[data-bbs-tab="community"]')`);await evaluate(`document.querySelector('[data-bbs-tab="community"]').click()`);await waitFor(`document.querySelector('#bbs-community-form')`);}
(async()=>{assert.ok(fs.existsSync(chromePath),'Chrome is required');db=await mysql.createConnection({host:process.env.DB_HOST,user:process.env.DB_USER,password:process.env.DB_PASS,database:process.env.DB_NAME,port:Number(process.env.DB_PORT||3306)});const[[admin]]=await db.query("SELECT EmployeeID,EmployeeName,Department,Unit,Position,Role FROM Employees WHERE EmployeeID=? AND LOWER(Role)='admin'",[process.env.PROD_UAT_ADMIN_ID]);const[[user]]=await db.query("SELECT e.EmployeeID,e.EmployeeName,e.Department,e.Unit,e.Position,e.Role FROM Employees e JOIN Master_Departments d ON LOWER(TRIM(d.Name))=LOWER(TRIM(e.Department)) WHERE e.EmployeeID=? AND LOWER(e.Role)<>'admin'",[process.env.PROD_UAT_USER_ID]);assert.ok(admin&&user,'Browser fixtures unavailable');chrome=spawn(chromePath,['--headless=new','--disable-gpu','--no-sandbox','--disable-dev-shm-usage','--disable-extensions','--no-first-run','--remote-allow-origins=*','--window-size=1600,1000',`--remote-debugging-port=${port}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','ignore'],windowsHide:true});let targets;for(let i=0;i<60;i++){try{const response=await fetch(`http://127.0.0.1:${port}/json`);if(response.ok){targets=await response.json();break;}}catch{}await sleep(250);}const page=targets?.find(x=>x.type==='page');assert.ok(page?.webSocketDebuggerUrl,'Chrome target unavailable');cdp=new Cdp(page.webSocketDebuggerUrl.replace('://localhost:','://127.0.0.1:'));await cdp.connect();await cdp.command('Page.enable');await cdp.command('Runtime.enable');await cdp.command('Page.addScriptToEvaluateOnNewDocument',{source:`window.API_BASE=${JSON.stringify(`${apiUrl}/api`)};`});await cdp.command('Page.navigate',{url:appUrl});await sleep(2000);await enter(user);const userState=await evaluate(`(()=>({form:Boolean(document.querySelector('#bbs-community-form')),risk:Boolean([...document.querySelectorAll('h3')].find(x=>x.textContent.includes('พฤติกรรมเสี่ยงและ Community Action'))),cards:Boolean([...document.querySelectorAll('h3')].find(x=>x.textContent.includes('บัตรรายแผนก'))),tabs:document.querySelectorAll('[data-bbs-tab]').length,overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth+2}))()`);assert.strictEqual(userState.form,true);assert.strictEqual(userState.risk,false,'User must not see Risky dashboard');assert.strictEqual(userState.cards,true);assert.strictEqual(userState.overflow,false,'BBS page must not overflow horizontally');await enter(admin);const adminState=await evaluate(`(()=>({risk:Boolean([...document.querySelectorAll('h3')].find(x=>x.textContent.includes('พฤติกรรมเสี่ยงและ Community Action'))),good:Boolean([...document.querySelectorAll('h3')].find(x=>x.textContent.includes('พฤติกรรมดีล่าสุด')))}))()`);assert.deepStrictEqual(adminState,{risk:true,good:true});await evaluate(`document.querySelector('[data-bbs-tab="cards"]').click()`);await waitFor(`document.querySelector('#bbs-dept-template-form') && document.querySelector('[data-community-handler]')`);assert.deepStrictEqual(errors,[],`Browser console errors: ${errors.join(' | ')}`);console.log('BBS Phase 8 browser Community visibility, Admin privacy and responsive layout UAT: PASS');})().catch(error=>{console.error(error.stack||error);process.exitCode=1;}).finally(async()=>{try{cdp?.close();}catch{}try{chrome?.kill();}catch{}if(db)await db.end();await fs.promises.rm(profile,{recursive:true,force:true}).catch(()=>{});});
