'use strict';
const fs=require('fs');const path=require('path');const assert=require('assert');const ui=fs.readFileSync(path.join(__dirname,'..','..','public','js','pages','bbs-smart-card.js'),'utf8'),main=fs.readFileSync(path.join(__dirname,'..','..','public','js','main.js'),'utf8');for(const marker of ["['community','Community / บัตรแผนก']",'bbs-community-form','departmentTemplateGallery','adminRiskPanel','data-dept-template-print','data-community-handler'])assert.ok(ui.includes(marker),`UI missing ${marker}`);assert.ok(ui.indexOf("if (state.tab === 'community')")<ui.indexOf('configurationReady'),'Community must render before formal BBS level gate');assert.match(main,/bbs_community_department_id/);assert.match(ui,/Community Report ไม่นับ KPI/);console.log('BBS Phase 8 UI/privacy contract: PASS');
