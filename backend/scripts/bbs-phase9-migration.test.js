'use strict';
const fs=require('fs');const path=require('path');const assert=require('assert');const read=name=>fs.readFileSync(path.join(__dirname,'..','migrations',name),'utf8');const sql=read('20260826_bbs_phase9_inspector_team_management.sql'),rollback=read('20260826_bbs_phase9_inspector_team_management.rollback.sql');for(const table of ['BBS_Inspector_Enrollments','BBS_Inspector_Team_Events'])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${table}`));assert.match(sql,/KpiRequired/);assert.match(sql,/AllowSelfManage/);assert.match(sql,/inspector_team_management_enabled/);assert.doesNotMatch(rollback,/DROP\s+TABLE/i);assert.match(rollback,/inspector_team_management_enabled'\s*,\s*'0'/);console.log('BBS Phase 9 migration contract: PASS');
