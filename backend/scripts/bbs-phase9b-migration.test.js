'use strict';
const fs=require('fs');const path=require('path');const assert=require('assert');const read=name=>fs.readFileSync(path.join(__dirname,'..','migrations',name),'utf8');const sql=read('20260826_bbs_phase9b_inspector_schedule.sql'),rollback=read('20260826_bbs_phase9b_inspector_schedule.rollback.sql');for(const table of ['BBS_Inspector_Schedule_Rules','BBS_Inspector_Schedule_Overrides','BBS_Inspector_Schedule_Events'])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${table}`));assert.match(sql,/UNIQUE KEY uq_bbs_schedule_override_date/);assert.match(sql,/inspector_schedule_enabled/);assert.doesNotMatch(rollback,/DROP\s+TABLE/i);assert.match(rollback,/inspector_schedule_enabled'\s*,\s*'0'/);console.log('BBS Phase 9B migration contract: PASS');
