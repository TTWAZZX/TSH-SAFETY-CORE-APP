'use strict';
const fs=require('fs');const path=require('path');const assert=require('assert');const root=path.join(__dirname,'..','..');const nodeRoute=fs.readFileSync(path.join(root,'backend','routes','bbs-inspector-schedules.js'),'utf8'),nodeService=fs.readFileSync(path.join(root,'backend','services','bbs-inspector-schedule.js'),'utf8'),node=nodeRoute+nodeService;const php=fs.readFileSync(path.join(root,'api','handlers','bbs_inspector_schedules.php'),'utf8');for(const route of ['/inspectors/compliance','/inspectors/:id/schedule','/admin/inspectors/:id/schedule'])assert(nodeRoute.includes(route),`Node route missing ${route}`);for(const token of ['inspectors/compliance','/schedule','schedule-overrides'])assert(php.includes(token),`PHP route missing ${token}`);for(const status of ['Completed','Partial','Missed','Upcoming','Exempt']){assert(node.includes(status));assert(php.includes(status));}
const {computeCompliance}=require('../services/bbs-inspector-schedule');const result=computeCompliance({enrollments:[{EnrollmentID:1,InspectorEmployeeID:'GL001',EnrollmentFrom:'2026-08-03',TargetCount:1,Weekdays:'1,2,3,4,5'}],rules:[],overrides:[{EnrollmentID:1,ScheduleDate:'2026-08-04',OverrideType:'Exempt',IsActive:1,Reason:'Training'},{EnrollmentID:1,ScheduleDate:'2026-08-08',OverrideType:'Required',TargetCount:2,IsActive:1,Reason:'Special'}],actualRows:[{ObserverEmployeeID:'GL001',ObservationDate:'2026-08-03',ActualCount:3},{ObserverEmployeeID:'GL001',ObservationDate:'2026-08-05',ActualCount:0},{ObserverEmployeeID:'GL001',ObservationDate:'2026-08-08',ActualCount:1}],range:{start:'2026-08-01',end:'2026-08-10'},today:'2026-08-08'});assert.equal(result.summary.numerator,2);assert.equal(result.summary.denominator,6);assert.equal(result.people[0].completedDays,1);assert.equal(result.people[0].partialDays,1);assert.equal(result.people[0].missedDays,3);assert.equal(result.people[0].days.find(row=>row.date==='2026-08-04').status,'Exempt');console.log('BBS Phase 9B Node/PHP route and schedule formula parity: PASS');
